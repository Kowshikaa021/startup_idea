# Startup Idea Evaluator 🚀

A mini AI web app to analyze startup ideas using LangChain + Gemini + LangGraph.

## Features
- Input your idea or upload a text file
- AI evaluates idea success chances
- Simple web interface (HTML/CSS + FastAPI)

## Tech Stack
- LangChain + Gemini API (Google Generative AI)
- LangGraph for workflow logic
- FastAPI backend
- HTML/CSS frontend

## How to Run Locally
```bash
pip install -r requirements.txt
uvicorn main:app --reload
